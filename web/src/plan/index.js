// Apollo v2 dashboard -- isometric floorplan surface public surface.
//
// The rest of the app (main.jsx, wired up by the orchestrator) should import
// `Plane` from here rather than reaching into the individual component files.

export { default as Plane } from './Plane.jsx';
